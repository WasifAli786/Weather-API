//Function for a iterators

const fruits = fruitIterator(myArray);

function func() {
    let x = fruits.next();
    if (x.value != undefined) {
        console.log(x.value);
        func();
    }
};

func();



//Function for hold option

(let btn = document.getElementById('btn');
btn.addEventListener('mousedown', func);
function func() {
    const inter = setInterval(() => {
        console.log('Hello');
    }, 200);
    // func1(inter);
    if(inter){
        btn.addEventListener('mouseup',()=>{
            clearInterval(inter)
        })
    }
};

function func1(timer){
    btn.addEventListener('mouseup',()=>{
        if (timer) {
            clearInterval(timer)
        }
    })
})